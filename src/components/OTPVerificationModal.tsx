// OTPVerificationModal removed — stub kept to avoid import errors if any remain.
import React from 'react';

const OTPVerificationModal: React.FC<any> = () => null;

export default OTPVerificationModal;